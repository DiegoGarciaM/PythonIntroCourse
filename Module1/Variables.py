# Podemos pensar que en cualquier lenguaje de programacion lo que escribimos son:
# Variables o datos
# Instrucciones

# Los datos son informacion, como valores de verdadero y falso, numeros, letras, etc.
# Podemos guardar informacion en la memoria mediante el uso de variables
# Las variables son basicamente solo poner nombre a un lugar en la memoria
# Y podemos poner algo en ese espacio de memoria, leerlo, asignarlo, cambiarlo

my_variable = "data"

# Ahora podemos acceder al contenido de my_variable en cualquier momento
# Tambien podemos actualizarlo o cambiarlo, y seguir usando el mismo nombre de variable


# En python los datos en la memoria tienen distintos tipos
# No todas las instrucciones son compatibles con todos los datos
# Ademas, distintos datos requieren mas o menos espacio en la memoria

# En python los tipos de datos mas sencillos son:
#   Los bool (booleanos, verdadero o falso:
bool_true = True
bool_false = False


#   Los numeros pueden ser enteros (int) o de punto flotante (float)
int_num = 128
float_num = 128.256


# Despues tenemos las secuencias, que son colleciones de datos, no solo uno
# Los string o cadenas de caracteres
string_var = 'This is a string'
